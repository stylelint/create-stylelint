export function showHelpAction(): void {
	console.log(`
Usage: create-stylelint [options]

Options:
-h, --help     Show help information
--dry-run      Run the command without making any changes
`);
}
